# Quick start
For Windows:
1. Prepare enviroment and build
``` console
    mkdir build
	cd build
	cmake ..
	cmake --build .
```
	
2. Run
``` console
    clox.exe
```
	
or run with cmake:
``` console
   cmake --build . --target run
```